const API_URL = 'http://localhost:5000/'

const API_ENDPOINTS = {
  courses: `${API_URL}courses`,
  register: `${API_URL}register`,
  login: `${API_URL}login`,
  insert: `${API_URL}insert`,
  course_kanji: `${API_URL}course_kanji/`,
  kanji: `${API_URL}kanji/`,
  user_courses: `${API_URL}user_courses/`,
  learning: `${API_URL}learning`,
  update_learning: `${API_URL}update_learning`,
  update_course: `${API_URL}update_course`
}

export default API_ENDPOINTS
